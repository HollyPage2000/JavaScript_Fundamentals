// Declare a function executeAfterDelay which calls the given function after the given delay in seconds:
function executeAfterDelay(func, delay) {
    setTimeout(func, delay * 1000);
}

// Declare a function execute

const printHello = () => {
    console.log('Hello!');
}

// Execute the function


executeAfterDelay(printHello, 3);